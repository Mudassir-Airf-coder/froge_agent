# FROGE Durable Context

**Purpose:** context-window handoff. This file is operational memory, not a chat transcript.

## Current objective
Finish FROGE as a verified local-first agent control plane that can manage approved tools, coordinate agents, use communication MCPs, manage reusable skills, update tools safely, and run a continuous evidence-driven implementation loop.

## Current baseline
The repository already contains a working bootstrap control plane, persistence, adapters, CLI, health ladder and universal install skill according to the latest implementation reports. Research documents are stored under `research/`. The repository also contains older root documentation that may describe earlier scope.

## New approved requirements
- MCPs are specifically for agent/tool communication and task delegation.
- Add automatic tool update detection and safe update/verify behavior.
- Add a dedicated Tool Expert for tool/plugin/integration research and setup.
- Add a dedicated Skill Designer/Skill Expert for finding an existing skill or designing a new reusable one when needed.
- Keep Ollama and vLLM excluded.
- Keep frontend excluded.
- Do not create unrelated MCPs, Omni Router MCP or Mega MCP unless explicitly re-authorized.
- Use verified research for concrete external install/update procedures.
- Use loop/harness engineering so the coding agent keeps implementing, testing, verifying and updating documentation until the defined scope is complete.

## Required continuity protocol
Before context exhaustion, update this file with: objective, completed work, active task, decisions, constraints, evidence, failures, files changed, and next exact action. Never store API keys, tokens, passwords or other secrets here.

## Current documentation source of truth
Read `docs/UPDATED-SYSTEM/README.md` first, then the documents in its stated order. Reconcile older root docs instead of silently contradicting them.
